# 🐳 Node.js + Dev Container

Este projeto é um exemplo simples de ambiente Node.js rodando dentro de um **Dev Container** (Docker) usando VS Code — sem necessidade de instalar Node.js ou dependências localmente.

## 🚀 Como usar

### 1. Pré-requisitos
- Docker Desktop
- Visual Studio Code
- Extensão: **Dev Containers** (ms-vscode-remote.remote-containers)

### 2. Executar
1. Abra este diretório no VS Code
2. Pressione `Ctrl+Shift+P` e selecione:

### 3. Executar o servidor

No terminal dentro do container:
```bash
npm start
```

### 4. Test

http://localhost:3000

